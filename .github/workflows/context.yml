name: Github Action Context

on: [push]

jobs:
  my-context:
    runs-on: ubuntu-latest
    steps:
      - name: my-context
        run: |
          echo Hello $MY_ACTOR
        env:
         MY_ACTOR: ${{ github.actor }} 